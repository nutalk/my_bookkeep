# Asset Initial Transaction Plan

## Files to Modify

### 1. `src/app/api/assets/route.ts` - POST handler
After inserting asset, create an `asset_income` initial transaction if `currentValue > 0`.

### 2. `src/app/assets/page.tsx`
Add `asset_value_change` handling in `txsWithBalance` and `chartData` computations.

## Changes

### assets/route.ts POST handler
Insert initial transaction after asset creation:
- type: `asset_income`
- amount: `body.currentValue`
- description: "初始金额"
- isAutoGenerated: true

### assets/page.tsx txsWithBalance
Add handling for `asset_value_change`:
- Backward walk: skip (absolute set cannot be cleanly undone)
- Forward walk: `balance = t.amount`

### assets/page.tsx chartData
Same `asset_value_change` handling as txsWithBalance.
